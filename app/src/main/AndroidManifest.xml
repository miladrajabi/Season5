<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.season5">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:name=".app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Episods.CustomContactActivity">
        </activity>
        <activity android:name=".Episods.CustomPermissionActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Episods.FragmentActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".adapter.CustomRecyclerDetailActivity" />
        <activity android:name=".Episods.CustomRecyclerActivity" />
        <activity android:name=".Episods.CustomGridViewActivity" />
        <activity android:name=".Episods.CustomListViewActivity" />
        <activity android:name=".Episods.MainActivity" />
    </application>

</manifest>